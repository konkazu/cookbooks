= DESCRIPTION:
chef 0.9 is fixed command timeout value and this is too short to finish command with file downloading.
chef 10.x can be set the timeout value but on 0.9.x we can set timeout value only by modify module.

= REQUIREMENTS:

= ATTRIBUTES:

= USAGE:

